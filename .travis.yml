dist: bionic
language: node_js
cache: yarn
node_js:
    - lts/*

install:
    - yarn install

script:
    - yarn dist

deploy:
    provider: script
    script: bash bin/deploy.sh $SFTP_USER $SFTP_PASSWORD $SFTP_HOST
